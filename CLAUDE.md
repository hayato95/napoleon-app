# napoleon-app — プロジェクト引継ぎメモ（Claude Code用）

技育CAMP2026 ハッカソンVol.5（9/17〜10/3, 5人チーム, 4人が開発未経験）向けプロジェクト。
トランプゲーム「ナポレオン」を、人間1人 × AI（CPU）4体で遊べるWebアプリとして実装する。

## 必読：機能要件定義書

FR-01〜FR-77 の全機能要件・優先度（Must/Should/Could）・担当区分・マイルストーン紐付けは全てここにある。
実装方針で迷ったら必ずここを確認すること。

https://claude.ai/code/artifact/052e8022-7794-41db-8eda-b85ef5af62f1

## 技術スタック（決定事項・変更しない）

| レイヤー | 選択 | 理由（要点） |
|---|---|---|
| フロントエンド | React（TypeScript） | チーム全員が同一言語で開発できることを最優先（9/28-30にHayatoが東京インターンで不在になるため、言語分断を避ける） |
| バックエンド | Node.js（TypeScript）＋ Socket.IO | 同上。トリックベースのリアルタイム通信にSocket.IOを使用 |
| DB | なし（サーバーメモリ内で状態管理） | MVPスコープでは永続化不要と判断。切断時の再接続はFR-59（reconnect ID）で対応 |

理由の詳細（比較検討した他の選択肢：Flutter+Firebase等）は要件定義書の「意思決定の経緯（ADR）」セクション参照。

## 現在地とマイルストーン

| マイルストーン | 期間 | 状態 | 関連FR |
|---|---|---|---|
| M0：環境構築・疎通確認・叩き台決定 | 9/21夜〜9/22朝 | **進行中** | FR-72（レイアウト叩き台） |
| M1：ルールエンジン構築 | 9/22〜9/23 | 未着手 | FR-01〜28, FR-74〜77 |
| M2：AI実装＋UI雛形＋チュートリアル素材 | 9/24〜9/25 | 未着手 | FR-29〜34, 71, FR-35〜44, 65, 66, FR-74〜77 |
| M3：統合（最重要チェックポイント） | 9/26〜9/27 | 未着手 | FR-45〜49, FR-72, 73 |
| M4：磨き込み（Hayato不在期間） | 9/28〜9/30 | 未着手 | FR-57〜64, FR-44, 65, 66 |
| M5：最終統合チェック | 10/1 | 未着手 | FR-50〜56, FR-61 |
| M6：デモ準備 | 10/2〜10/3 | 未着手 | - |

**M0の残タスク（今すぐやること）**：
1. `frontend/`にReact（TypeScript）プロジェクトを初期化（`npm create vite@latest . -- --template react-ts` 推奨）
2. `backend/`にNode.js（TypeScript）＋ Socket.IOプロジェクトを初期化
3. React⇔Node.js/Socket.IOの疎通確認（最小構成でイベント1往復できればOK）
4. `.github/workflows/ci.yml`はすでに雛形があるので、package.json追加後に動作確認
5. GitHubのブランチ保護ルールを設定（1件以上のレビュー承認必須、`main`への直push禁止）
6. FR-72（画面レイアウト叩き台）をチームで決定

## 開発ルール

- `main`への直pushは禁止。必ずPull Requestを経由する
- PRのマージには1件以上のレビュー承認が必須
- **コミット・PR作成時、Co-Authored-By等のAI署名は一切付与しない**（Hayatoさんの明示的な運用ルール）
- 担当（フロントエンド/バックエンド/両方）は要件定義書に記載があるが、厳密に縛らず柔軟に分担してよい方針で合意済み

## 進め方の指針

- 各機能はFR番号で要件定義書と対応させ、実装時はFR番号をコミットメッセージやPRタイトルに含める（例：`feat: FR-13 副官指名の実装`）
- CPU AIは「常に最適解を打つ最強NPC」にしない。確率的な揺らぎ（20%/15%程度の非最適手）を入れる設計が要件定義書のFR-29〜34に明記済み
